<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", buches_path  %></li>
      <% if can? :manage, @buches %>
        <li><%= link_to "#{t("web-app-theme.new")}", new_buch_path %></li>
      <% end %>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("models.books.all")  %></h2>
    <%= render :partial => "list_books", :locals => { :books => @buches} %>
  <% content_for :sidebar, render(:partial => 'sidebar_search', :locals => {:search => @search}) -%>
  </div>
</div>
